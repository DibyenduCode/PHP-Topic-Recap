--create table and its collam

CREATE TABLE students(
    id int NOT NULL AUTO_INCREMENT,
    name varchar(30),
    class varchar(30),
    course varchar(30),
    year varchar(30),
    PRIMARY KEY(id)
);